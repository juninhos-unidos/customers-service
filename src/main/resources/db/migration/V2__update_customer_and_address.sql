ALTER TABLE address DROP CONSTRAINT IF EXISTS fk_customer;

ALTER TABLE customer DROP COLUMN IF EXISTS id;
ALTER TABLE customer ADD COLUMN id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE address DROP COLUMN IF EXISTS id;
ALTER TABLE address DROP COLUMN IF EXISTS customer_id;

ALTER TABLE address
    ADD COLUMN id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    ADD COLUMN customer_id BIGINT NOT NULL,
    ALTER COLUMN number TYPE VARCHAR(20),
    ALTER COLUMN number SET NOT NULL,
    ALTER COLUMN country TYPE VARCHAR(100),
    ALTER COLUMN country SET NOT NULL;

ALTER TABLE address
    ADD CONSTRAINT fk_customer
    FOREIGN KEY (customer_id)
    REFERENCES customer(id)
    ON DELETE CASCADE;
